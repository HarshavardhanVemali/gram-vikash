const fetch = require('node-fetch');

const API_KEY = process.env.EXPO_PUBLIC_GEMINI_API_KEY; // I'll need to pass this or hardcode it if I have it, wait I don't have the key.
console.log("Waiting for key...");
